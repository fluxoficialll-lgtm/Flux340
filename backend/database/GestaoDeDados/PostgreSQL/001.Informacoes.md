# Documentação da Camada de Consultas (Gestão de Dados)

O diretório `/backend/database/GestaoDeDados/PostgreSQL` atua como uma camada intermediária entre os `Serviços` e os `Repositorios`. Sua principal função é preparar, validar e gerenciar os dados antes que eles sejam enviados para a camada de acesso ao banco de dados (o Repositório).

## Estrutura e Organização

A nomenclatura `Consultas.{Dominio}.{Subdominio}.js` mantém a consistência com o resto da arquitetura, deixando claro qual parte da aplicação cada arquivo serve.

### Principais Responsabilidades:

- **Validação de Dados de Entrada:** Antes de uma operação de banco de dados ser executada, esta camada realiza validações para garantir que os dados estão no formato correto e atendem aos requisitos necessários. Por exemplo, verifica se um ID é um número válido, se campos obrigatórios não estão vazios, etc.

- **Preparação de Dados:** Pode ser responsável por transformar ou preparar os dados recebidos da camada de `Serviços` para o formato exato que a camada de `Repositorio` espera.

- **Encapsulamento da Lógica de Acesso:** Funciona como um ponto central para a lógica de consulta. Em vez de a camada de `Serviços` chamar diretamente o `Repositorio`, ela chama esta camada de `Consultas`. Isso permite adicionar validações ou lógica de preparação sem precisar modificar a camada de `Serviços`.

- **Delegação para o Repositório:** Após a validação e preparação, esta camada delega a execução final da query para a camada de `Repositorio` correspondente.

## Fluxo de Dados (Típico)

1.  A camada de `Serviços` decide que precisa interagir com o banco de dados e chama uma função nesta camada de `Consultas`, passando os dados do usuário ou da requisição.
2.  A função de `Consultas` valida os dados recebidos. Se a validação falhar, ela pode lançar um erro (`AppError`).
3.  Se a validação for bem-sucedida, a função chama a função correspondente na camada de `Repositorio` (ex: `repositorioPublicacaoFeed.criar(dados)`).
4.  O `Repositorio` executa a query e retorna o resultado.
5.  A camada de `Consultas` recebe o resultado e o retorna para a camada de `Serviços`.

Essa camada adiciona um nível extra de abstração e robustez, garantindo que apenas dados válidos e bem formados cheguem à camada de interação direta com o banco de dados.
